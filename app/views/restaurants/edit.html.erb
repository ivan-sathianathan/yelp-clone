<%= form_for @restaurant do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit %>
<!-- the submit should send it to the 'update' method in the RestaurantsController file -->
<% end %>
